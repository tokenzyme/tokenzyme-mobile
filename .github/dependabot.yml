version: 2

# Minor and patch bumps are batched into one reviewable PR per ecosystem.
# Major bumps are deliberately excluded from the group so each arrives on its own:
# a major is a migration, not an update, and batching one in with harmless bumps
# makes the whole group unmergeable.

updates:
  - package-ecosystem: pub
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 5
    groups:
      safe-updates:
        patterns:
          - '*'
        update-types:
          - minor
          - patch

  - package-ecosystem: gradle
    directory: /android
    schedule:
      interval: monthly
    open-pull-requests-limit: 5
    groups:
      safe-updates:
        patterns:
          - '*'
        update-types:
          - minor
          - patch

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 5
    groups:
      safe-updates:
        patterns:
          - '*'
        update-types:
          - minor
          - patch
